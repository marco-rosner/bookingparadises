import { createTheme } from "@mui/material";

export const theme = createTheme({
    palette: {
      primary: {
        main: "#000"
      },
      secondary: {
        main: '#fff'
      },
      text: {
        primary: '#000',
        secondary: '#fff',
      },
    },
  })