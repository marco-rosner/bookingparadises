import React from "react";
import { Skeleton } from "@mui/material";

export const TitleSkeleton = (): React.ReactElement => (
    <Skeleton width="30%" sx={{ marginLeft: '30px', fontSize: '2rem'}} />
)