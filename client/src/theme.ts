import { createTheme } from "@mui/material";

export const theme = createTheme({
    palette: {
      primary: {
        main: "#0288d1"
      },
      secondary: {
        main: '#fff'
      },
      text: {
        primary: '#000',
        secondary: '#fff',
      },
    },
  })